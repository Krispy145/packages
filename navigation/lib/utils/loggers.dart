/// [NavigationLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum NavigationLoggers {
  /// [navigation] is the feature reference for navigation.
  navigation,
}
