/// [MapLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum MapLoggers {
  /// [map] is the feature reference for map.
  map,

  /// [markers] is the feature reference for markers.
  markers,

  /// [search] is the feature reference for search.
  search,
}
