import 'package:utilities/data_sources/source.dart';

import '../../models/map_model.dart';

/// [MapDataSource] is an abstract class that defines the basic CRUD operations for the [MapModel] entity.
abstract class MapDataSource extends DataSource<MapModel> {}
