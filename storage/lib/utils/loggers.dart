/// [StorageLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum StorageLoggers {
  /// [storage] is the feature reference for storage.
  storage,

  /// LOGGER FEATURE END
}
