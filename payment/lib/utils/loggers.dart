/// [PaymentLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum PaymentLoggers {
  /// [payment] is the feature reference for payment.
  payment,
  stripe

  /// LOGGER FEATURE END
}
