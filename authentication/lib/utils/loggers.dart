/// [AuthenticationLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum AuthenticationLoggers {
  /// [authentication] is the feature reference for authentication.
  authentication,
}
