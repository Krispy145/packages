/// [AuthenticationPackageLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum AuthenticationPackageLoggers {
  /// [authentication] is the feature reference for authentication.
  authentication,
}
